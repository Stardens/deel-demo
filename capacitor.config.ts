import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitordemo.dangjo',
  appName: 'capacitor-demo-dangjo',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
